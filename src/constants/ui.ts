export const StyledVariant = {
  Standard: 'standard',
  Outlined: 'outlined',
};

export const StyledVariantList = Object.values(StyledVariant);

export const StyledColor = {
  Primary: 'primary',
  Secondary: 'secondary',
};

export const StyledColorList = Object.values(StyledColor);

export const StyledSize = {
  Small: 'small',
  Medium: 'medium',
  Large: 'large',
};

export const StyledSizeList = Object.values(StyledSize);

export const StyledPosition = {
  TopLeft: 'top-left',
  TopRight: 'top-right',
  BottomLeft: 'bottom-left',
  BottomRight: 'bottom-right',
};

export const StyledPositionList = Object.values(StyledPosition);
