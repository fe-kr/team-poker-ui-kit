export enum StyledVariant {
  Standard = 'standard',
  Outlined = 'outlined',
}

export enum StyledColor {
  Primary = 'primary',
  Secondary = 'secondary',
}

export enum StyledSize {
  Small = 'small',
  Medium = 'medium',
  Large = 'large',
}

export enum StyledPosition {
  Left = 'left',
  Right = 'right',
}
